# Prompt Log
## Задание М2:  Генератор и анализатор паролей
### Промпт 1
**Инструмент:** GigaCode
**Промпт:** "Создай файл password_tools.py. Реализуй в нем функцию generate_password(length: int = 12, use_digits: bool = True, use_special: bool = True) -> str. Функция должна использовать модуль secrets для безопасности. Если параметры True, добавляй в набор символов цифры и знаки пунктуации."
**Результат:** "Файл password_tools.py успешно создан с функцией generate_password, использующей модуль secrets для безопасной генерации паролей. Функция поддерживает параметры длины, включения цифр и специальных символов."
### Промпт 2
**Промпт:** "Добавь в password_tools.py функцию check_strength(password: str) -> str. Она должна возвращать:

'Weak', если длина < 8 или используется только один тип символов.

'Medium', если длина >= 8 и есть буквы + цифры.

'Strong', если длина >= 12, есть буквы разного регистра, цифры и спецсимволы."
**Результат:** "Файл password_tools.py успешно обновлён. В нём теперь находятся две функции:

generate_password(length: int = 12, use_digits: bool = True, use_special: bool = True) -> str — генерирует безопасный пароль с использованием модуля secrets.
check_strength(password: str) -> str — оценивает надёжность пароля по заданным критериям и возвращает 'Weak', 'Medium' или 'Strong'."
### Промпт 3
**Промпт:** "Создай файл test_medium_02.py для тестирования функций из password_tools.py с помощью pytest. Проверь:

Соответствие длины сгенерированного пароля.

Наличие спецсимволов, если они включены.

Корректность оценки 'Weak', 'Medium' и 'Strong' для разных паролей."
**Результат:** "Файлы password_tools.py и test_medium_02.py успешно созданы и содержат требуемый функционал.


password_tools.py включает две функции:


generate_password() — безопасная генерация паролей с помощью secrets.
check_strength() — оценка надёжности пароля по заданным критериям.


test_medium_02.py содержит тесты с использованием pytest:


Проверка длины сгенерированного пароля.
Проверка наличия/отсутствия спецсимволов в зависимости от параметра.
Проверка корректности оценок Weak, Medium, Strong."
### Промпт 4
**Промпт:** "Исправь функцию check_strength в password_tools.py. Тесты падают по следующим причинам:

Пароль из одного типа символов (например, "abcdefgh") должен считаться 'Weak', даже если его длина 8. Сейчас он возвращает 'Medium'.

Пароль "Abc123!@#" должен считаться 'Strong'. Сейчас он возвращает 'Medium' (вероятно, из-за проверки длины >= 12). Снизь порог для 'Strong' до 8 символов, если соблюдены все условия по типам символов (регистр, цифры, спецсимволы)."
**Результат:** "Готово. Были созданы и доработаны файлы:


password_tools.py — содержит функции:


generate_password() — генерирует безопасный пароль с заданными параметрами.
check_strength() — оценивает надёжность пароля как 'Weak', 'Medium' или 'Strong' по уточнённым критериям.


test_medium_02.py — содержит тесты pytest, проверяющие:


Длину генерируемых паролей.
Наличие/отсутствие спецсимволов в зависимости от параметра.
Корректность оценки надёжности для разных типов паролей, включая граничные случаи.

Критерии оценки силы пароля:

'Weak': длина < 8 или используется только один тип символов.
'Medium': длина >= 8 и минимум два типа символов (например, буквы и цифры).
'Strong': длина >= 8, присутствуют буквы разного регистра, цифры и спецсимволы.
Все тесты обновлены и соответствуют новой логике."
### Итого
- Количество промптов: 4
- Что пришлось исправлять вручную: ничего
- Время: ~30 мин
## Задание М5:Адресная книга
### Промпт 1
**Инструмент:** GigaCode
**Промпт:** "Создай файл expense_tracker.py. Реализуй класс ExpenseTracker. Внутри должен быть список для хранения расходов. Реализуй метод add_expense(amount: float, category: str, description: str, date: str), где date — строка в формате 'YYYY-MM-DD'. Метод должен сохранять расход в виде словаря или объекта."
**Результат:** "Файл expense_tracker.py успешно создан и реализован класс ExpenseTracker с методом add_expense, который сохраняет расходы в виде словаря.

Готов к следующим задачам, если нужно расширить функциональность (например, добавить фильтрацию, удаление, сохранение в файл и т.д.)."
### Промпт 2
**Промпт:** "Добавь в класс ExpenseTracker два метода:

get_total(category=None) -> float: возвращает общую сумму расходов. Если указана категория — только по ней.

get_by_category() -> dict: возвращает словарь, где ключи — названия категорий, а значения — суммарные траты в них."
**Результат:** "Файл expense_tracker.py успешно создан и дополнен. Реализован класс ExpenseTracker со следующими методами:

add_expense(amount: float, category: str, description: str, date: str) — добавляет расход в виде словаря.
get_total(category=None) -> float — возвращает общую сумму расходов, с возможностью фильтрации по категории.
get_by_category() -> dict — возвращает словарь с суммами расходов по каждой категории."
### Промпт 3
**Промпт:** "Добавь в класс ExpenseTracker:

get_by_date_range(start: str, end: str) -> list: возвращает список расходов за период (включая границы). Используй datetime.strptime для сравнения дат.

get_top_categories(n=5) -> list: возвращает список из n самых затратных категорий (название, сумма), отсортированный по убыванию суммы."
**Результат:** "Файл expense_tracker.py успешно создан и реализован с требуемой функциональностью. Класс ExpenseTracker включает следующие методы:

add_expense(amount: float, category: str, description: str, date: str) — добавляет расход в виде словаря.
get_total(category=None) -> float — возвращает общую сумму расходов, с фильтрацией по категории (опционально).
get_by_category() -> dict — возвращает словарь с суммами по категориям.
get_by_date_range(start: str, end: str) -> list — возвращает расходы в заданном диапазоне дат, используя datetime.strptime.
get_top_categories(n=5) -> list — возвращает топ-N самых затратных категорий, отсортированных по убыванию."
### Промпт 4
**Промпт:** "Создай test_medium_05.py для тестирования ExpenseTracker через pytest. Проверь:

Корректность фильтрации по категории.

Правильность подсчета общей суммы.

Фильтрацию по диапазону дат.

Что get_top_categories возвращает именно топ по сумме, а не по количеству записей."
**Результат:** "Файл test_medium_05.py успешно создан и содержит полный набор тестов для класса ExpenseTracker с использованием pytest. Тесты проверяют:

test_category_filter: корректность фильтрации расходов по категории и подсчета суммы.
test_total_sum: правильность подсчета общей суммы всех расходов.
test_date_range_filter: фильтрацию расходов по диапазону дат с использованием get_by_date_range.
test_top_categories_by_amount_not_count: убеждение, что get_top_categories возвращает категории, отсортированные по сумме расходов, а не по количеству записей.
Тесты покрывают ключевые сценарии, включая проверку приоритета суммы над количеством операций. Файл готов к запуску через pytest."
### Итого
- Количество промптов: 4
- Что пришлось исправлять вручную: ничего
- Время: ~20 мин