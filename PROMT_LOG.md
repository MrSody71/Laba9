# Prompt Log
## Задание М2:  Генератор и анализатор паролей
### Промпт 1
**Инструмент:** GigaCode
**Промпт:** "Создай файл password_tools.py. Реализуй в нем функцию generate_password(length: int = 12, use_digits: bool = True, use_special: bool = True) -> str. Функция должна использовать модуль secrets для безопасности. Если параметры True, добавляй в набор символов цифры и знаки пунктуации."
**Результат:** "Файл password_tools.py успешно создан с функцией generate_password, использующей модуль secrets для безопасной генерации паролей. Функция поддерживает параметры длины, включения цифр и специальных символов."
### Промпт 2
**Промпт:** "Добавь в password_tools.py функцию check_strength(password: str) -> str. Она должна возвращать:

'Weak', если длина < 8 или используется только один тип символов.

'Medium', если длина >= 8 и есть буквы + цифры.

'Strong', если длина >= 12, есть буквы разного регистра, цифры и спецсимволы."
**Результат:** "Файл password_tools.py успешно обновлён. В нём теперь находятся две функции:

generate_password(length: int = 12, use_digits: bool = True, use_special: bool = True) -> str — генерирует безопасный пароль с использованием модуля secrets.
check_strength(password: str) -> str — оценивает надёжность пароля по заданным критериям и возвращает 'Weak', 'Medium' или 'Strong'."
### Промпт 3
**Промпт:** "Создай файл test_medium_02.py для тестирования функций из password_tools.py с помощью pytest. Проверь:

Соответствие длины сгенерированного пароля.

Наличие спецсимволов, если они включены.

Корректность оценки 'Weak', 'Medium' и 'Strong' для разных паролей."
**Результат:** "Файлы password_tools.py и test_medium_02.py успешно созданы и содержат требуемый функционал.


password_tools.py включает две функции:


generate_password() — безопасная генерация паролей с помощью secrets.
check_strength() — оценка надёжности пароля по заданным критериям.


test_medium_02.py содержит тесты с использованием pytest:


Проверка длины сгенерированного пароля.
Проверка наличия/отсутствия спецсимволов в зависимости от параметра.
Проверка корректности оценок Weak, Medium, Strong."
### Промпт 4
**Промпт:** "Исправь функцию check_strength в password_tools.py. Тесты падают по следующим причинам:

Пароль из одного типа символов (например, "abcdefgh") должен считаться 'Weak', даже если его длина 8. Сейчас он возвращает 'Medium'.

Пароль "Abc123!@#" должен считаться 'Strong'. Сейчас он возвращает 'Medium' (вероятно, из-за проверки длины >= 12). Снизь порог для 'Strong' до 8 символов, если соблюдены все условия по типам символов (регистр, цифры, спецсимволы)."
**Результат:** "Готово. Были созданы и доработаны файлы:


password_tools.py — содержит функции:


generate_password() — генерирует безопасный пароль с заданными параметрами.
check_strength() — оценивает надёжность пароля как 'Weak', 'Medium' или 'Strong' по уточнённым критериям.


test_medium_02.py — содержит тесты pytest, проверяющие:


Длину генерируемых паролей.
Наличие/отсутствие спецсимволов в зависимости от параметра.
Корректность оценки надёжности для разных типов паролей, включая граничные случаи.

Критерии оценки силы пароля:

'Weak': длина < 8 или используется только один тип символов.
'Medium': длина >= 8 и минимум два типа символов (например, буквы и цифры).
'Strong': длина >= 8, присутствуют буквы разного регистра, цифры и спецсимволы.
Все тесты обновлены и соответствуют новой логике."
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~30 мин
